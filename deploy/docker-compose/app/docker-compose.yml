version: "3.8"

services:
  sparkinai-app-web:
    build:
      context: ../../..
      dockerfile: deploy/docker/Dockerfile
    image: sparkinai-app-web:latest
    container_name: sparkinai-app-web
    restart: unless-stopped
    ports:
      - "19520:80"
    environment:
      - TZ=Asia/Shanghai
