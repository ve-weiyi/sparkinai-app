version: "3"

services:
  # 前端服务
  ve-blog-element:
    build:
      context: ../
      dockerfile: deploy/Dockerfile
    image: shadcn-vue-demo:latest  # 设置新的镜像名称和标签
    container_name: shadcn-vue-demo
    restart: always
    ports:
      - "5173:80"
